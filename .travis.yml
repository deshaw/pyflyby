language: python

sudo: false
python:
  - 2.7

matrix:
  include:
    - python: 2.7
    - python: 3.7
      dist: xenial
      sudo: true

  allow_failures:
    - python: 3.7
      dist: xenial
      sudo: true

install:
  - set -e
  - pip install "pexpect>=3.3" pyflakes pytest epydoc rlipython requests jupyter
  - pip list

script:
  - set -e
  - set -x
  - pytest --doctest-modules lib tests
